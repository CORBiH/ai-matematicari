# --- Core web ---
Flask>=3.0
gunicorn>=23.0.0
flask-cors>=4.0
flask-limiter>=3.5.0
python-dotenv>=1.0

# --- OpenAI / HTTP ---
openai>=1.40.0
requests>=2.31.0

# --- Excel content (modular data: master + Thinkific mapa) ---
openpyxl>=3.1

# UKLONJENO (Phase 2 audit — NULA importa u kodu, provjereno grep-om):
#   numpy, sympy, matplotlib, scikit-learn, argon2-cffi, PyJWT, psycopg2-binary
#
# UKLONJENO (2026-07-14, brisanje legacy /submit stacka):
#   google-auth, google-cloud-storage, google-cloud-firestore, google-cloud-tasks,
#   gspread — koristio ih je ISKLJUČIVO legacy async/Sheets put, koji je obrisan.
#   Tutor loguje u SQLite (matbot/activity_log.py) i ne dira GCP.
# Rollback: `git revert` commita koji je uklonio legacy stack.
